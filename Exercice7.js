/**
 *l'addition de deux nombres
 *
 * @param {number} a le premier nombre .
 * @param {number} b le deuxième nombre.
 * @returns {number} la somme de a et b.
 */
const ajouter = (a, b) => a + b;
//test

console.log(ajouter(3 ,5));